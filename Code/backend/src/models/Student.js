export default class Student {
 constructor(id, name, email, nfcTagId, currentLocation, currentClass) {
 this.id = id;
 this.name = name;
 this.email = email;
 this.nfcTagId = nfcTagId;
 this.currentLocation = currentLocation;
 this.currentClass = currentClass;
 }
}