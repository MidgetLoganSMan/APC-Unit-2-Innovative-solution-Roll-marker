export default class Class {
 constructor(id, className, teacherId, room, period) {
    this.id = id;
    this.className = className;
    this.teacherId = teacherId;
    this.room = room;
    this.period = period;
 }
}